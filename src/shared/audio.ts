/**
 * Phase 2 target: factor out the Tone.js signal chain (drone + melody synth +
 * reverb + lowpass + oscilloscope analyser) from modes/01-seven-rooms.html
 * and the other lab lessons that duplicate it.
 *
 * Intentionally empty in Phase 1 — nothing imports it yet. Keeping the file
 * present so the shared/ namespace is already wired and Phase 2 migrations
 * don't have to touch tsconfig paths or vite.config.ts.
 */
export {};
